git --version - проверка гита
git config --global user.name "Имя Фамилия"
git config --global user.email "Почта"
git config --list - проверка настроек гита
git log - посмотреть все коммиты
git cat-file -p номер хэша - посмотреть содержимое объекта
git branch -m "Название ветки" - переименование текущей ветки
git branch -d "Название ветки" - удалить ветку (Прежде чем удалить надо из нее выйти)

"CREATE git"
git init - новый репозиторий
git status - статус git

"COMMIT"
git add . - затрекать все изменения
git commit -m "Текст" - Коммит файлов и добавление названия коммита
git checkout -b develop - создание ветки разработки
git checkout -b features/cc-100-helloworld - создание ветки функций и функции cc-100-helloworld

"Reset"
git reset хэш-номер коммита - откатиться к нужной версии

"Merge"
git checkout develop -Перейти на ветку develop
git merge features/cc-100-helloworld - Имя ветки с которой нужно слить в ветку develop



1. git init
2. git remote add origin https://github.com/buligar/interest.git
3. git branch -M main
4. git add . - затрекать все изменения
5. git commit -m "Текст" - Коммит файлов и добавление названия коммита
6. git push -u origin main
Ввести логин
Ввести API токен
